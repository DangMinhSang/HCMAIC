# Kaggle normally already provides numpy and torch.  None of these packages
# downloads an AIC dataset; datasets must be attached with Kaggle Add Input.
numpy>=1.26
# Kaggle already ships a CUDA-matched PyTorch build. Installing/upgrading torch
# from PyPI here can replace its NCCL libraries and break ``libtorch_cuda``.
# For a non-Kaggle local install, install the matching PyTorch wheel separately.
Flask>=3.0
gradio>=5.0
Pillow>=10.0
# The optional Kaggle-only Qwen reranker is installed by run.py from
# requirements-reranker.txt. Keep local installs lightweight.
transformers>=4.44
deep-translator>=1.11
ftfy>=6.1
regex>=2023.0
tqdm>=4.65
git+https://github.com/openai/CLIP.git
